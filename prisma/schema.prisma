// prisma/schema.prisma
// Phase 1 baseline: PostgreSQL datasource and Prisma Client only.
// Application-owned models belong to EPIC-002 Phase 2.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
